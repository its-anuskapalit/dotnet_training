using System;
class Program
{
    public static void Main(String[] args)
    {
        //class for checking a number is even or not
        //Cal.isEven();
        //Even.check();

        //Height.check();
        //Height Category: Accept height in cm. If < 150 (Dwarf), 150-165 (Average), 165-190 (Tall), >190 (Abnormal).
        
        //Maximum.check();
        //Largest of Three: Take three integers and find the maximum using nested if.

        //LeapYear.Checker();
        // Determine if a year is leap

        //discriminate.cal();
        //Calculate roots of $ax^2 + bx + c = 0$ using if-else to check the discriminant.

        //Admission.Eligibility();
        //Math ≥ 65, Phys ≥ 55, Chem ≥ 50 AND (Total ≥ 180 OR Math+Phys ≥ 140).
        
        //bill.cal();
        //Electricity Bill: Calculate bill: First 199 units @ 1.20; 200-400 @ 1.50; 400-600 @ 1.80; above 600 @ 2.00. Add 15% surcharge if bill > 400.

        //vowel.check();
        //Vowel or Consonant: Use a switch statement to check if a character is a vowel.
        //Triangle.Type();
        //quadrant.find();


        //LOOP
        //prime.check();
        //primeAll.check();
        //reverse.num();
        //conti.Usage();
        //pascal.Display();
        //armstrong.check(); do
        //fibonacci.display();

    }   
    }

